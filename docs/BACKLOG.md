# Что осталось сделать — передача второму разработчику

Статус на 17.09.2026, после сдачи трека A и каркаса трека C. Канонический список задач; ID требований — из `docs/TZ-IT-School-RTK.md` и дополнения `docs/ТЗ-по-итогам-хакатона.md`.

**Приоритет источников:** требования хакатона уточняют базовое ТЗ. В частности,
текущее поведение `A6`, при котором карточка остаётся на версии маршрута, с которой
началась работа, подлежит расширению задачами `A8–A12`: для активных заявок должна
быть доступна контролируемая миграция на новую версию.

---

## Базовая линия: что уже работает

Сделан **SPEC-01** (`first-move-spec.md`) — ядро данных, **трек A — workflow-движок**, плюс подключён Keycloak.

| Блок | Состояние |
|---|---|
| Справочники: вузы, вендоры, ИТ-направления, ИТ-продукты, контакты вузов | CRUD + мягкое удаление + поиск, сортировка, пагинация |
| `interactions` — карточка «вуз + направление + продукт» | CRUD, бизнес-ключ, upsert |
| Импорт xls/xlsx (`SRV-01`, все 10 полей `FLD-01…10`) | Загрузка → маппинг → предпросмотр → запись → отчёт об ошибках |
| Аудит (`audit_log`) | Автоматический, append-only, ПДн маскируются |
| Роли и видимость (`FR-12`) | `user` / `manager` / `admin`, фильтр на уровне репозитория |
| Keycloak (`FR-11`) | Realm `crm`, роли, вход через Swagger, JWT проверяется по JWKS |
| Коды ошибок (`NFR-03`) | Единый конверт, 9 кодов |
| Swagger (`SOL-02`) | Все методы описаны, перечень библиотек в README |
| Workflow-движок (`WF-BASE`, `FR-03`, `FR-04`, `FR-07`, `FR-10`) | Маршруты, версии, этапы, переходы с комментариями, вложения, схема для фронтенда; перенос файлов из БД в MinIO запланирован задачей `F11` |
| Интеграции (`FR-06`, `UC-U-02`) | Каркас приёма на демо-данных; контракт API за заказчиком, см. `docs/INTEGRATIONS.md` |
| Docker (`SOL-04`) | `docker compose up` — postgres + keycloak + api, миграции в entrypoint |

Проверено: 223 теста, `ruff check`, `ruff format --check`, `mypy app` — чисто.
Голова миграций: `0005_integration_syncs`. Запуск: `docker compose up`.
OpenAPI выгружен в `docs/openapi.json` (49 путей, 79 операций): `python -m scripts.export_openapi`.

---

## Как подключаться к существующему коду

Три вещи, которые экономят день на старте.

**Аудит достаётся бесплатно.** Унаследуй модель от `DomainBase` (`backend/app/models/base.py`) — получишь UUID-PK, `created_at/updated_at/created_by/updated_by`, мягкое удаление и автоматическую запись в `audit_log` на каждый insert/update. Ничего вызывать руками не нужно. Для не-ORM событий (переход по этапу, выгрузка файла) — `backend/app/services/audit.py::log_event`.

**Видимость достаётся бесплатно.** Если новая таблица привязана к вузу, задай в репозитории `university_scope_column = "university_id"` — и роль `user` автоматически будет видеть только свои записи. Фильтр живёт в `BaseRepository`, роутер про него не знает.

**Не затирай заполненное пустым.** Правило «пустая ячейка = нет данных» действует во всём импорте — см. `InteractionService.upsert`. Тот же принцип нужен переходам и интеграциям.

Полезные точки входа: `backend/app/services/catalogs.py` (find-or-create для всех справочников — вызывать из интеграций, а не дублировать), `backend/app/services/text.py` (нормализация названий), `backend/app/core/errors.py` (коды ошибок).

---

## Трек A. Workflow-движок — базовая версия **сделана**, нужны уточнения хакатона

Реализовано целиком: миграция `0004_workflow_engine`, заглушки `workflow_version_id`
и `current_stage_id` стали настоящими внешними ключами.

| ID | Задача | Состояние |
|---|---|---|
| **A1** | Схема workflow: шаблон → версия → этапы → переходы, история, комментарии | Готово |
| **A2** | Базовый процесс из 14 шагов, предзаполнен и редактируем | Готово, `backend/app/services/workflow_presets.py` |
| **A3** | Переходы с проверкой допустимости и обязательным комментарием | Готово, код ошибки `WORKFLOW_INVALID_TRANSITION` |
| **A4** | Переименование этапов, в том числе в опубликованной версии | Готово |
| **A5** | Вложения в 10 форматах, тип по сигнатуре файла | Готово; временно хранятся в БД, перенос в MinIO — `F11` |
| **A6** | Версионирование: правка не переписывает путь карточек в работе | Готово, карточка помнит версию старта |
| **A7** | Схема маршрута для фронтенда одним запросом | Готово, `/workflows/{id}/graph` и `/interactions/{id}/route` |

### Доработки по итогам хакатона — приоритет P0

| ID | Задача | Требование | Критерий приёмки |
|---|---|---|---|
| **A8** | Назначение workflow для карточки взаимодействия с вузом | `H-WF-02`, `H-REQ-01` | Новые карточки взаимодействия с вузами создаются по назначенному маршруту; изменение маршрута не влияет на уже созданные карточки. |
| **A9** | Добавить предварительный просмотр и подтверждённую миграцию активных заявок при публикации новой версии workflow | `H-WF-03`, `H-WF-05` | До сохранения показаны затрагиваемые заявки и сопоставление «старый этап → новый этап»; без подтверждения данные не меняются. |
| **A10** | Сделать безопасное удаление этапа с обязательным выбором целевого этапа | `H-WF-04` | Удаление заблокировано, пока не выбран этап назначения; после подтверждения все заявки перенесены в одной транзакции. |
| **A11** | Ограничить переименование опубликованного этапа ролью `admin`; добавить явное подтверждение опасных операций | `H-WF-05`, `H-WF-06`, `H-UI-03` | Пользователь и руководитель не могут переименовать этап; интерфейс показывает последствия перед переименованием, удалением и миграцией. |
| **A12** | Дополнить аудит событиями миграции, массового перевода и изменения этапа | `H-WF-07`, `H-SEC-05` | В журнале есть автор, время, старое/новое значение и идентификаторы либо число затронутых заявок. |

`A6` остаётся полезным механизмом хранения версий и истории. `A9` не должен удалять
старые версии: он добавляет управляемое сопоставление для активных карточек.

**Что ещё не закрыто:** заказчик пока не определил **срок хранения вложений** — файлы лежат в базе бессрочно, автоочистки нет.

## Трек B. Отчёты и статистика — **сделано**

`EVF-03` проверяет отчёты напрямую, а `EVP-03` — ещё на предварительной экспертизе.

| ID | Задача | Требование | Критерий приёмки |
|---|---|---|---|
| **B1** | Фильтр по периоду в `/interactions` | `FR-01` | Готово: `period_from` / `period_to`, период действия лицензии пересекается с указанным интервалом |
| **B2** | Конструктор отчётов: выбор колонок из пяти (вуз, ИТ-направление, ИТ-продукт, статус работы с вузом, ответственный), выгрузка в xls, xlsx, pdf | `FR-05`, `FR-09`, `UC-U-03` | Готово: `POST /reports/export`, ответ — файл |
| **B3** | Диаграммы и графики с экспортом в png и pdf | `FR-02` | Готово: `POST /reports/statistics`; данные строятся по тому же запросу, что и табличный отчёт |
| **B4** | Асинхронная генерация отчётов | `NFR-07` | Готово: CPU-генерация вынесена в пул потоков; нагрузочный критерий покрывается отдельным тестом F1 |
| **B5** | Формирование результирующего JSON-файла | `SOL-04` | Готово: формат `json` у `/reports/export` |

Колонка «статус работы с вузом» берётся из текущего этапа карточки: `interactions.current_stage_id` → `workflow_stages.name`. Трек A готов, заглушка по `transfer_status` больше не нужна.

---

## Трек C. Интеграции — **каркас сделан**, подмена ждёт заказчика

Весь путь приёма реализован и работает на демонстрационных данных: забор по HTTP
с пагинацией и токеном, разбор, сопоставление, запись через сервисный слой,
постановка на workflow, предпросмотр, история запусков, аудит. По допущению принята
только форма входящего JSON.

Подробности, принятые допущения и порядок подмены на реальный API —
**`docs/INTEGRATIONS.md`**.

| ID | Задача | Требование | Состояние |
|---|---|---|---|
| **C1** | Приём данных из LMS по API в формате JSON | `FR-06`, `UC-U-02` | Каркас готов; подставить реальный контракт |
| **C2** | Приём данных с сайта на CMS Laravel | `FR-06`, `UC-U-02` | Каркас готов; подставить реальный контракт |
| **C3** | Полученные данные добавляются в workflow | `FR-06` | Готово: новые карточки встают на маршрут по умолчанию |

**Что осталось:** получить от заказчика `OPEN-01` (описание API) и `OPEN-04`
(перечень полей), поправить `backend/app/integrations/contract.py`, прописать адреса,
проверить предпросмотром на боевых данных, удалить демо-фикстуры. Отдельно не
решены: способ авторизации, если он не Bearer-токен; частота и инициатор
синхронизации; нужна ли отдача данных обратно.

## Трек D. Фронтенд — **брать параллельно**

Без него нет `DLV-03` (работающий прототип с веб-интерфейсом) и половины критериев `EVF-04`. Стек по ТЗ — React.

| ID | Задача | Требование |
|---|---|---|
| **D1** | Каркас SPA + вход через Keycloak (PKCE, обновление токена) | `FR-13`, `FR-11` |
| **D2** | Справочники и мастер импорта в 5 шагов — API готов полностью | `SRV-01` |
| **D3** | Визуализация пути взаимодействия, переходы, комментарии, вложения | `FR-03`, `FR-04` — API готов целиком |
| **D4** | Фильтры, отчёты, диаграммы | `FR-01`, `FR-02`, `FR-05` |
| **D5** | Кэш действий пользователя | `FR-14` |
| **D6** | Десктоп + мобильная версия, без перезагрузки страницы при изменениях | `UX-04`, `NFR-02` |
| **D7** | Экран создания карточки взаимодействия с вузом, выбор workflow и безопасные диалоги изменения маршрута | `H-WF-02`, `H-WF-05`, `H-UI-03` |
| **D8** | Интерфейс чата в карточке взаимодействия | `H-MSG-01…03` |

Для `D6` обязательна проверка сценариев входа, списка, карточки, смены этапа и отправки сообщения на ширине экрана от 360 px (`H-UI-01`).

---

## Трек E. Права и администрирование — сделано

| ID | Задача | Требование |
|---|---|---|
| **E1** | Маппинг ролей ТЗ закреплён: «Пользователь» = `user` (КАМ), «Руководитель» = `manager`, «Администратор» = `admin`. `manager` управляет ответственными за вузы, `admin` — политиками видимости | `FR-12` |
| **E2** | Админ-настройки ограничений видимости: режимы `assignments` (закреплённые вузы), `selected` (явный список) и `all`; проверка работает на уровне репозитория для всех выборок | `UC-A-01` |
| **E3** | В development realm заведено 20 КАМов; сидирование заранее создаёт их локальные проекции по стабильным Keycloak UUID, а вход по проверенному JWT поддерживает актуальность данных | `FR-11`, `FR-12` |

---

## Трек F. Нефункциональные требования и эксплуатация

| ID | Задача | Требование |
|---|---|---|
| **F1** | Нагрузочные тесты: 50 параллельных пользователей, 10 параллельных отчётов, отклик ≤ 1 с на ключевых операциях | `NFR-01`, `NFR-06`, `NFR-07` |
| **F2** | CI: прогон `ruff`, `mypy`, `pytest` на каждый пуш — сейчас пайплайна нет | `EVP-02` |
| **F3** | Продовый контур Keycloak: реальный realm, секреты вне репозитория, HTTPS. Локальный `docker/keycloak/realm-export.json` — только для разработки | `FR-11` |
| **F4** | Меры ИБ по 152-ФЗ и приказу ФСТЭК № 117: заполнить матрицу мер, зафиксировать, что закрыто аудитом и мягким удалением, а что требует инфраструктуры | `FR-08`, `SOL-10` |
| **F5** | Партиционирование `audit_log` по месяцам и политика ретенции `import_rows` (там лежат ФИО контактов вузов) | — (техдолг SPEC-01) |
| **F6** | Оптимизация dry-run импорта: сейчас на каждую строку идут отдельные запросы за вузом и менеджером. На файле в тысячи строк это заметно — нужен кэш в рамках задачи импорта | `NFR-01` |
| **F7** | Серверный кэш повторно используемых данных: Redis или KeyDB, отключаемый конфигурацией; не реализовывать офлайн-редактирование и отложенную синхронизацию без отдельного согласования | `H-CACHE-01…03` |
| **F8** | Feature flags для необязательных модулей: уведомления, внешние каналы, чат и кэш | `H-SEC-06` |
| **F9** | Проверить развёртывание в закрытом контуре: отсутствие общего исходящего интернета, allowlist IP/портов LMS и CMS, работа через VPN/прокси и с клиентским TLS-сертификатом | `H-SEC-02…04`, `H-UI-02` |
| **F10** | Матрица ПДн: перечень полей списков участников, цели обработки, роли доступа, маскирование журналов и политика хранения | `H-SEC-01`, `H-REQ-02` |
| **F11** | Подключить MinIO как S3-совместимое объектное хранилище для файлов, приложенных к этапам workflow, карточкам, сообщениям чата и будущим файловым сущностям | `FR-04`, `H-MSG-01`, `H-SEC-01` | Файл загружается в MinIO; в БД остаются метаданные и объектный ключ, а скачивание проходит с проверкой прав доступа к исходной сущности. Поддержаны текущие 10 форматов вложений. |
| **F12** | Мигрировать существующие файлы вложений из БД в MinIO и исключить хранение бинарного содержимого в прикладных таблицах | `F11` | Скрипт миграции идемпотентен, формирует отчёт о результатах, не теряет метаданные и обеспечивает чтение ранее загруженных файлов после миграции. |

Для `F11` необходимо добавить сервис MinIO в `docker compose`, вынести endpoint, bucket,
access key и secret key в переменные окружения, создать bucket при старте или отдельной
инициализацией и не публиковать MinIO напрямую в интернет. Доступ к объектам должен
осуществляться через API приложения либо короткоживущие подписанные URL после авторизации.

---

## Трек G. Документация и сдача

| ID | Задача | Требование |
|---|---|---|
| **G1** | Руководство пользователя и руководство системного администратора со скриншотами интерфейса, **встроенные в платформу** | `NFR-05` |
| **G2** | Описание функциональной и компонентной архитектуры **в Archi** | `SOL-07` |
| **G3** | Презентация (pptx или pdf) и ссылка на работающий прототип | `PRES-01`, `DLV-02`, `DLV-03` |
| **G4** | Сопроводительная документация: методы обработки данных, условия и ограничения решения, инструкции по сборке и установке | `SOL-06`, `SOL-07`, `DLV-04` |
| **G5** | Офлайн-руководство пользователя, доступное из продукта без внешних ресурсов | `H-DOC-01` |
| **G6** | Руководство администратора: закрытый контур, Keycloak, роли, allowlist интеграций, кэш, feature flags и каналы уведомлений | `H-DOC-02` |
| **G7** | Предоставить команде тестовый доступ к целевому контуру до защиты и проверить сценарий подключения | `H-DOC-03` |

---

## Трек H. Коммуникации и образовательные данные — **не начат**

Задачи трека выполнять после `A8` и параллельно с фронтендом. Модульные границы
должны позволять отключить чат и внешние каналы feature flag'ами (`F8`).

| ID | Задача | Требование | Критерий приёмки |
|---|---|---|---|
| **H1** | Модель правил уведомлений по переходам workflow: событие, адресат, роль и канал | `H-NOT-01`, `H-NOT-04` | Для перехода можно настроить хотя бы один канал и получателя: ответственный, руководитель, роль или пользователь. |
| **H2** | Адаптеры каналов email, Telegram и MAX с очередью повторной отправки и аудитом ошибок | `H-NOT-02` | Ошибка внешнего канала не отменяет переход заявки; задача отправки повторяется и логируется. |
| **H3** | Планировщик контроля зависших заявок с порогом 14 дней по умолчанию и настраиваемыми адресатами | `H-NOT-03`, `H-NOT-04` | При включённом правиле создаётся одно уведомление о заявке, превысившей порог пребывания на этапе. |
| **H4** | Backend чата в карточке: сообщения, автор, время, проверка области доступа и аудит | `H-MSG-01…03` | Пользователь с доступом к карточке читает и создаёт сообщения; пользователь без доступа получает отказ. |
| **H5** | Каталоги обучающихся, преподавателей и образовательных активностей с минимальными ПДн | `H-REQ-02`, `H-SEC-01` | У активности есть список участников, содержащий ФИО и email; видимость ограничена ролью и вузом. |
| **H6** | Сквозное расписание активностей по федеральным проектам и программам | `H-REQ-03` | В расписании выводятся период, активность, вуз/проект и связанные участники. |

---

## Решения, которые принимает заказчик, а не разработчик

Пока не закрыты — держать на виду.

| Вопрос | Почему важно |
|---|---|
| Хеширование ПДн в аудите без соли | Несолёный sha256 телефона или email теоретически подбирается по словарю. Альтернатива — HMAC с ключом, но тогда хеши нельзя сравнивать между записями |
| Состав полей, отнесённых к ПДн | Сейчас `full_name`, `email`, `phone`. Если заказчик добавит, например, должность — это одна строка в модели |
| Срок хранения `import_rows` | Там лежат сырые строки Excel с ФИО контактов вузов. Автоочистки сознательно нет |
| Срок хранения вложений к этапам | Файлы лежат в БД бессрочно. Само место хранения выбрано (база, не том и не S3), автоочистки нет |
| Политика хранения файлов в MinIO | Для `F11–F12` нужно согласовать срок хранения, версионирование, резервное копирование и необходимость антивирусной проверки загружаемых файлов |
| `OPEN-01`, `OPEN-04` | Контракты API LMS и сайта, перечень полей JSON |
| Право переименования этапа | До отдельного решения реализовать только для `admin` (`A11`, `H-OPEN-01`) |
| Сетевой доступ к Telegram/MAX и email из закрытого контура | Определяет, какие адаптеры `H2` могут быть включены в демонстрации (`H-OPEN-02`) |
| Получатель напоминания по умолчанию | Предлагается ответственный и его руководитель; необходимо подтвердить (`H-OPEN-03`) |
| Формат чата | Для первой версии — текст; вложения и упоминания требуют отдельного решения (`H-OPEN-05`) |

---

## Рекомендуемый порядок

1. ~~**A1–A7**, **B1–B5**, **E1–E3** — базовые возможности готовы.~~
2. **A8–A12** — первыми: они задают корректную модель взаимодействия с вузами и безопасную миграцию существующих заявок. Без них нельзя считать workflow соответствующим уточнённому ТЗ.
3. Параллельно **D1, D3, D6–D7**: интерфейс маршрутов, подтверждения опасных действий и адаптивная версия. API для существующего workflow уже есть, API `A8–A12` необходимо добавить по мере реализации.
4. После модели карточки — **H1–H4**: уведомления, напоминания и чат. Внешние каналы включать только после проверки ограничений закрытого контура.
5. Затем **H5–H6**, **F7–F12**, **G5–G7**. `F11` нужен до включения вложений в чат и до нагрузки на рабочий контур; `F12` выполнять после успешной проверки MinIO. Интеграции `C1–C3` подменять на реальные контракты после получения данных от заказчика.
